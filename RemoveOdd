#Function that removes odd numbers from a list

def remove_odd(lst):
    res = []
    for ele in lst:
        if ele % 2 == 0:
            res.append(ele)
    return res

print remove_odd([3,5,4,1,6])